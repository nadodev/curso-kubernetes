apiVersion: v1
kind: Service

metadata:
  name: "goserver"
  labels:
    app: "goserver"

spec:
  selector:
    app: "goserver"
  ports:
    - protocol: "TCP"
      port: 80
      targetPort: 8080
  type: "NodePort"